{
  "configurations": [
    {
     "name": "Python Debugger: Remote Attach",
        "type": "debugpy",
        "request": "attach",
        "port": 5678,
        "host": "localhost",
        "pathMappings": [
        {
          "image": "accounts-api",
          "containerName": "accounts-api",
          "sourceFileMap": {
            "${workspaceFolder}": "/app"
          }
        }
      ]
    }
  ]
}
